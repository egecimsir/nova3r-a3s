# Core dependencies (required for import and inference)
torch>=2.2
torchvision
numpy
einops
scipy
tqdm
opencv-python
Pillow

# Config / checkpoint loading
omegaconf

# Diffusion / flow-matching primitives
diffusers
flow-matching
torchdiffeq

# HuggingFace checkpoint hub
huggingface-hub>=0.22

# Point sampling (used by nova3r.utils.sampling and the triposg AE)
torch-cluster
# pytorch3d  # required only for FPS/k-NN sampling paths; install separately

# Optional: PLY I/O for save_pointcloud_ply / predict(output_path=...)
# open3d
